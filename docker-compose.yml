version: "3.4"
services:
  web:
    build:
      context: ./
      dockerfile: Travsystem.Web/Dockerfile
    image: jockesv/travsystem-web
    networks: 
      - webnet

networks:
  webnet:
    driver: overlay
    attachable: true    